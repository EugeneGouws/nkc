# Copy this file to .env and fill in your values.
# IMPORTANT: Use APIFY_KEY (no VITE_ prefix) — this is read server-side by the Netlify function,
# NOT embedded in the browser bundle.

APIFY_KEY=your_apify_api_token_here
